title CPMS employee login

actor CPMS employee
participant CPMS_userInterfaceManager
participant CPO_API
participant CPMS_userInterfaceManager
participant CPMS employee
CPMS employee->CPMS_userInterfaceManager:login(data)
activate CPMS_userInterfaceManager
CPMS_userInterfaceManager->CPMS_userInterfaceManager:ValidateInput()
alt isValid
CPMS_userInterfaceManager->CPO_API:checkEmployee(data)
	activate CPO_API
	alt validEmployee
    CPO_API-->CPMS_userInterfaceManager:validEmployee
    deactivate CPO_API
    CPMS_userInterfaceManager-->CPMS employee:Successfull login
    else User already registered
 	activate CPO_API
    CPO_API-->CPMS_userInterfaceManager:invalidEmployee
    deactivate CPO_API
    CPMS_userInterfaceManager-->CPMS employee:Failed
    space
end
else notValid
CPMS_userInterfaceManager-->CPMS employee:Invalid data
space 
end 